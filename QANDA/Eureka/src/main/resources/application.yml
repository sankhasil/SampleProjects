spring:
  application:
    name: serviceRegistry
server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
logging:
  file: './log/serviceRegistry.log'
